spring:
  application:
    name: app.registration
  cloud:
    config:
      uri: http://localhost:8080

server:
  port: 8080

eureka:
  client:
    registerWithEureka: true
    fetchRegistry: true
    serviceUrl:
      defaultZone: http://localhost:9002/eureka/
  instance:
    hostname: virtualkube
